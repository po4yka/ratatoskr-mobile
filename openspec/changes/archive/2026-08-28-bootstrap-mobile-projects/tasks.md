## 1. Buildable shared, Android, and iOS shells

- [x] 1.1 Add `tooling/tests/bootstrap_structure_test.sh` with the failing test `android_shell_hosts_shared_compose`: assert the settings expose `:shared` and `:androidApp`, the Android shell delegates to one shared Compose entry point, and the requested catalog/convention-plugin structure exists; run it and confirm the purpose-specific missing Android/KMP foundation assertion fails before implementation.
- [x] 1.2 Add the Gradle wrapper, worker cap, version catalog, convention plugins, `:shared` Android/iOS targets, shared Compose root, thin Android application shell, Kotlin/Compose lint configuration, and minimal resources; rerun `bootstrap_structure_test.sh` and `build-gate -- ./gradlew --no-daemon :shared:testDebugUnitTest :androidApp:assembleDebug` and confirm both pass.
- [x] 1.3 Extend `tooling/tests/bootstrap_structure_test.sh` with the failing test `ios_shell_hosts_shared_compose`: assert a shared UIKit controller factory, committed Ratatoskr Xcode project/scheme, SwiftUI wrapper, and Kotlin framework direct-integration phase; run it and confirm the purpose-specific missing iOS integration assertion fails before implementation.
- [x] 1.4 Add the thin SwiftUI/UIKit iOS shell and Xcode project/scheme that embed the shared Compose controller through ObjC framework interop; rerun `bootstrap_structure_test.sh`, then run `build-gate -- ./gradlew --no-daemon :shared:iosSimulatorArm64Test :shared:linkDebugFrameworkIosSimulatorArm64` and `build-gate -- xcodebuild -project iosApp/Ratatoskr.xcodeproj -scheme Ratatoskr -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO build`, confirming the simulator framework and app build.

## 2. KMP ownership decision

- [x] 2.1 Add `tooling/tests/architecture_boundary_test.sh` with the failing test `adr_0001_assigns_every_required_boundary`: assert ADR-0001 assigns shared Compose/navigation/UDF, public models/client, and queue rules to KMP and lifecycle/share/security/background/file/notification/accessibility work to native adapters, with rationale and ObjC/Swift Export direction; run it and confirm it fails because the ADR is absent.
- [x] 2.2 Add accepted `docs/adr/0001-kmp-boundary.md`, update the ADR index and architecture overview to match the selected shared-Compose/native-lifecycle boundary, and rerun `architecture_boundary_test.sh` until it passes. Documentation cannot itself start from a unit test; the executable responsibility-matrix check supplies the red/green evidence.

## 3. Generated Platform contract models and drift enforcement

- [x] 3.1 Commit Platform OpenAPI revision `a117ca854e73e7b6a0acbc0844905ca53462a502`, document digest `7eec085cf2bfeb80c10d78a0aa23c7a2febebe30352dd69765c746e9022bec28`, add the pinned OpenAPI Generator model-only configuration/lock manifest, generate the owned Kotlin kotlinx.serialization model tree twice into temporary directories, verify byte equality, commit it under the shared generated source set, and verify `:shared:compileDebugKotlinAndroid` succeeds. This task cannot start from a failing unit test because the inputs, generator configuration, and outputs are generated/configuration files.
- [x] 3.2 Add `shared/src/commonTest/.../GeneratedContractSmokeTest.kt` with `capability_document_round_trips_synthetic_public_response`, deserialize and reserialize a synthetic response represented by the pinned document, and run `build-gate -- ./gradlew --no-daemon :shared:testDebugUnitTest :shared:iosSimulatorArm64Test`; this is a conformance check for generated code, so it is added immediately after the generator-owned output rather than manufacturing a compile-error red state.
- [x] 3.3 Add `tooling/tests/contract_drift_test.sh` with `mutated_pin_is_rejected` and `stale_generated_model_is_rejected`; run it against the initial generated-tree comparison and confirm the suite fails specifically because a valid JSON mutation outside the generated model surface is wrongly accepted, while the stale generated file is rejected.
- [x] 3.4 Add provenance digest enforcement before generation/diff, keep checks read-only by generating only in temporary directories, and rerun `contract_drift_test.sh` plus the clean drift command until mutation, stale-output, determinism, missing-output, and orphan-output cases all behave as specified.

## 4. Product gate and hosted CI

- [x] 4.1 Add `tooling/tests/gate_parity_test.sh` with the failing tests `ci_runs_shared_tests` and `documented_gate_matches_ci`: assert the shared Android/iOS test commands and Android/iOS build, lint, contract, mutation, and OpenSpec command families appear in both `DEVELOPMENT.md` and `.github/workflows/ci.yml`; run it and confirm the missing product CI/gate assertions fail.
- [x] 4.2 Add exact local commands to `DEVELOPMENT.md`, Swift format/lint and Gradle Kotlin lint tasks, Linux Android/shared-contract and macOS iOS jobs in `.github/workflows/ci.yml`, dependency/Gradle caches keyed by lock inputs, and strict OpenSpec validation; rerun `gate_parity_test.sh` and validate the workflow syntax/configuration.
- [x] 4.3 Update README/bootstrap status and `docs/IMPLEMENTATION_PLAN.md` evidence wording without claiming feature UI, real-device, signing, provider, or live Platform proof. Documentation cannot start from a failing test; verify the repository status descriptions against the delivered modules and gate commands.

## 5. Full validation and handoff readiness

- [x] 5.1 Run the targeted repository tests first, then every documented Gradle and Xcode build/test command through `build-gate`, and run `openspec validate --all --strict` plus `openspec validate --archived`; record only observed green commands and leave the task unticked if any command is skipped or fails.
- [x] 5.2 Review `git diff --check`, the complete scoped diff, generated provenance, dependency pins, Android/iOS artifacts, CI command parity, privacy/security boundaries, and unrelated-work preservation; fix any defect, rerun affected checks, and confirm the change is ready for archive, commit, integration, push, exact hosted-CI verification, and cleanup.
