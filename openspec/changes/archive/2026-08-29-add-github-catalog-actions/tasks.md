## 1. Pinned GitHub interaction contracts

- [x] 1.1 RED: add `tooling/tests/github_contract_fixture_test.sh::pinned_github_schemas_and_fixtures_match_the_reviewed_contract_revision`; run it before adding the pin and observe the exact missing lock/schema/fixture assertion rather than a shell/tool failure.
- [x] 1.2 GREEN: pin the four reviewed `ratatoskr-contracts` GitHub interaction schemas, all corresponding valid/invalid fixtures, producer commit, and per-file digests under `contracts/github/`; extend contract check/drift wiring, mutate one copied valid fixture in a temporary directory and observe the drift invocation reject it, then rerun `github_contract_fixture_test.sh`, `build-gate -- ./tooling/contracts/check.sh`, and `build-gate -- ./tooling/tests/contract_drift_test.sh` to green. Configuration and copied contract artifacts have no additional meaningful behavior RED beyond the missing/drift assertions.
- [x] 1.3 RED: add the compile-only public codec seam and `shared/src/commonTest/kotlin/com/ratatoskr/mobile/github/GithubContractCodecTest.kt` tests `valid_preview_and_partial_action_fixtures_decode_exactly`, `unknown_or_unsafe_preview_members_are_rejected`, `invalid_action_reason_or_aggregate_is_rejected`, and `accepted_backup_is_not_projected_as_completed`; run the focused shared JVM test and observe behavioral decode/projection assertions fail against the empty seam, not missing symbols or malformed fixture loading.
- [x] 1.4 GREEN: implement strict transport-private Kotlin serialization DTO validation and closed domain projections for preview target/actions and all three action-result components; rerun `GithubContractCodecTest` to green.

## 2. Current capability projection and authenticated GitHub adapter

- [x] 2.1 RED: add the compile-only GitHub service-capability projection seam and extend `DeviceSessionManagerTest` with `current_valid_github_service_projects_preview_and_closed_actions`, `missing_stale_or_malformed_github_service_is_unavailable`, and `capability_refresh_replaces_previous_github_actions`; run the focused shared JVM test and observe the valid service remains unavailable or stale actions survive incorrectly.
- [x] 2.2 GREEN: strictly project one non-stale `github` service document into `CapabilitySnapshot`, preserve no opaque service JSON beyond projection, clear it on revocation/sign-out, and rerun `DeviceSessionManagerTest` to green.
- [x] 2.3 RED: add compile-only `PlatformGithubApi`/authorized repository seams and `shared/src/commonTest/kotlin/com/ratatoskr/mobile/github/PlatformGithubApiTest.kt` tests `preview_posts_canonical_contract_body_to_platform`, `action_posts_exact_target_confirmation_and_idempotency`, `redirect_and_invalid_contract_responses_fail_closed`, `action_disconnect_after_send_is_outcome_unknown`, and `unauthorized_recovery_and_revocation_are_distinct`; run the focused test and observe request/result assertions fail against unavailable responses.
- [x] 2.4 GREEN: implement Ktor preview/action calls to `/v1/gh/repositories/preview` and `/v1/gh/repositories/actions`, redirect refusal, bounded decoding, transport classification, and `DeviceAuthorizedRequestExecutor` recovery; rerun `PlatformGithubApiTest` to green.

## 3. Fixture catalog browse and search

- [x] 3.1 RED: add the compile-only fixture catalog/store seam and `shared/src/commonTest/kotlin/com/ratatoskr/mobile/github/GithubCatalogStoreTest.kt` tests `empty_query_browses_stable_fixture_order_without_network`, `bounded_case_insensitive_query_filters_name_and_description`, `over_limit_query_preserves_last_results`, and `unpaired_missing_stale_or_malformed_capability_exposes_no_actions`; run the focused test and observe the empty store/capability placeholder fails state and zero-call assertions.
- [x] 3.2 GREEN: implement the deterministic reset-on-restart fixture rows, bounded local search, explicit fixture authority, pairing/capability states, and live preview loading on row selection without any inferred catalog-list call; rerun `GithubCatalogStoreTest` to green.

## 4. One-shot confirmation and truthful action state

- [x] 4.1 RED: add the compile-only detail/action store seam and `shared/src/commonTest/kotlin/com/ratatoskr/mobile/github/GithubConfirmationStoreTest.kt` tests `track_selection_only_discloses_ratatoskr_tracking_until_confirmed`, `star_selection_names_repository_account_and_external_write_until_confirmed`, `cancel_replay_and_replaced_confirmation_send_nothing`, and `target_account_or_capability_change_invalidates_pending_confirmation`; run the focused test and observe premature/missing requests or reusable pending state assertions fail.
- [x] 4.2 GREEN: implement immutable preview fingerprints, precise track/star prompt projections, consume-before-launch confirm/cancel handling, current-capability revalidation, and injected bounded confirmation/idempotency identity creation; rerun `GithubConfirmationStoreTest` to green.
- [x] 4.3 RED: add `shared/src/commonTest/kotlin/com/ratatoskr/mobile/github/GithubActionOutcomeStoreTest.kt` tests `partial_result_preserves_metadata_star_and_backup_failure`, `accepted_policy_never_claims_backup_complete`, `inconsistent_aggregate_is_invalid_response`, `outcome_unknown_retry_reuses_same_idempotency_key`, and `revocation_transitions_to_repairing`; run the focused test and observe flattened success, lost identity, or wrong auth-state assertions fail.
- [x] 4.4 GREEN: implement pessimistic action submission, same-key explicit uncertain retry, independent component/result presentation, safe reasons, invalid-response and re-pairing states; rerun `GithubActionOutcomeStoreTest` to green.

## 5. Shared Compose surface in thin Android and iOS shells

- [x] 5.1 RED: add `androidApp/src/androidTest/kotlin/com/ratatoskr/mobile/github/GithubCatalogUiTest.kt` tests `fixture_browse_search_and_detail_are_visibly_unsynchronized`, `track_and_star_show_distinct_confirmation_effects_before_dispatch`, `partial_fixture_renders_all_component_facts`, and `loading_empty_unavailable_reauth_invalid_and_outcome_unknown_states_are_visible`; run the focused emulator test through `build-gate` and observe missing GitHub navigation/semantics rather than infrastructure failure.
- [x] 5.2 GREEN: add shared Navigation 3 GitHub catalog/detail routes and Compose surfaces, expose the feature from paired home, wire the Android application graph, keep submissions out of recomposition, and rerun `GithubCatalogUiTest` plus all common GitHub suites to green.
- [x] 5.3 RED: extend `shared/src/iosTest/kotlin/com/ratatoskr/mobile/IosApplicationGraphTest.kt` with `github_graph_shares_device_authorization_capabilities_and_fixture_browse_without_provider_credentials`; run `build-gate -- ./gradlew --no-daemon :shared:iosSimulatorArm64Test` and observe the graph is absent or violates the request/credential assertions.
- [x] 5.4 GREEN: wire the same long-lived GitHub graph into the iOS shared controller without Swift feature UI, entitlement, or provider storage changes; rerun iOS KMP tests, the hosted Xcode test scheme, and the unsigned iOS simulator build to green.

## 6. CI, documentation, and evidence boundaries

- [x] 6.1 RED: extend `tooling/tests/gate_parity_test.sh` to require GitHub contract drift, common codec/capability/transport/store suites, Android GitHub instrumentation, and iOS graph coverage in both `DEVELOPMENT.md` and `.github/workflows/ci.yml`; run it before changing either source and observe each missing command/name diagnostic.
- [x] 6.2 GREEN: update CI and `DEVELOPMENT.md` so hosted Android and iOS jobs run the new shared/native tests and contract mutation gate, retain existing build/test artifacts, and rerun `gate_parity_test.sh` to green. CI configuration has no additional behavior RED beyond the parity test.
- [x] 6.3 Update README, implementation-plan item 7, architecture/interfaces/testing/threat-model documentation, and evidence labels for live paired preview/actions versus unsynchronized fixture browse/search; documentation cannot start from a meaningful behavior test, so verify the exact boundaries, endpoint names, lack of permissions/entitlements/provider credentials, and remaining live/device gaps with `rg` and rendered Markdown review.

## 7. Full gate, review, and OpenSpec completion

- [x] 7.1 Run every command in `DEVELOPMENT.md`, routing all Gradle and Xcode builds/tests through `build-gate`, including contract mutation/drift, Kotlin/Swift lint, Android/JVM and iOS Simulator common tests, Android emulator instrumentation, full hosted XCTest scheme, both application builds, strict OpenSpec validation, `git diff --check`, and relevant action/workflow lint; record exact outcomes and do not relabel fixture/simulator evidence as live GitHub, physical-device, Vault, or provider proof.
- [x] 7.2 Review the complete diff for invented catalog endpoints/capabilities, provider credentials or direct GitHub calls, permissive contract decoding, reusable/stale confirmation, idempotency loss, false backup claims, fixture/live authority confusion, sensitive diagnostics, recomposition writes, unsafe text, unrelated changes, and stale call sites; fix every finding and rerun its narrow test plus affected gate.
- [x] 7.3 Sync the `mobile-github-catalog-actions` delta into main specs, verify delta/main equivalence, rerun `openspec validate --all --strict` and `openspec validate --archived`, and confirm every task is checked before archive and the requested commit/integrate/push/exact-SHA-hosted-check/cleanup lifecycle.
