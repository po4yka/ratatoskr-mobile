## 1. Public full-text search transport and state

- [x] 1.1 RED: extend the compile-only library API/repository seams and add `PlatformLibraryApiTest.search_sends_trimmed_query_and_exact_page_bounds`, `search_rejects_blank_oversized_or_invalid_page_without_request`, and `search_preserves_ranked_generated_page`; run the focused common test and observe unavailable/empty placeholder results fail the URL, zero-call, and decoded-order assertions rather than compilation.
- [x] 1.2 GREEN: implement the pinned `GET /v1/library/search` query transport, generated `LibraryPage` validation, redirect/auth/retry classification, and authorized repository mapping without tenant selectors or a second endpoint; rerun the task 1.1 tests plus existing recent/read-state transport tests to green.
- [x] 1.3 RED: add a compile-only `LibrarySearchStore` and `shared/src/commonTest/kotlin/com/ratatoskr/mobile/library/LibrarySearchStoreTest.kt` cases `valid_query_renders_live_results_in_platform_order`, `blank_oversized_and_missing_capability_send_nothing`, `duplicate_load_more_appends_one_page_once`, `stale_older_query_cannot_replace_newer_results`, `offline_retry_keeps_query_without_results`, and `unauthorized_requires_repairing`; run the focused common test and observe state/request-generation assertions fail against the placeholder store.
- [x] 1.4 GREEN: implement the generation/query/offset-fenced UDF search store with explicit idle/loading/empty/offline/unavailable/re-pairing/content states, one in-flight next page, explicit retry, and no fixture/cache substitution; rerun task 1.3 and all library store tests to green.

## 2. Shared search presentation and application wiring

- [x] 2.1 RED: add `androidApp/src/androidTest/kotlin/com/ratatoskr/mobile/library/LibrarySearchUiTest.kt` cases `search_flow_renders_ranked_live_results_and_loads_next_page_once`, `validation_capability_empty_offline_and_repairing_states_are_truthful`, and `stale_response_never_replaces_current_query`; run the focused emulator test through `build-gate` and observe the missing route/field/state semantics fail rather than emulator infrastructure.
- [x] 2.2 GREEN: add the shared Navigation 3 search route/surface, wire it from the paired home/library graph in both thin shells, keep submission out of composition, and rerun task 2.1 plus common search/library tests to green.

## 3. Exact custom, App Link, and Universal Link routing

- [x] 3.1 RED: extend the compile-only route configuration/keys and `ContentRouteTableTest` with table tests `canonical_https_analysis_collection_and_repository_routes_resolve`, `custom_scheme_routes_remain_compatible`, and `scheme_host_port_credentials_encoding_traversal_query_fragment_identifier_and_extra_segment_variants_are_rejected`; run the focused common test and observe accepted-route assertions fail while every pre-existing custom route still passes.
- [x] 3.2 GREEN: implement raw-string exact parsing with build-configured ASCII lowercase hosts plus analysis UUID, collection slug, and repository owner/name destinations; wire truthful fixture/integration-pending collection and authenticated repository/analysis navigation, then rerun task 3.1 and all routing tests.
- [x] 3.3 RED: add `AndroidAppLinkIntentTest.canonical_configured_https_routes_forward_raw_and_foreign_or_ambiguous_links_fail_closed` plus manifest/shell parity assertions for one `autoVerify` HTTPS host placeholder; run focused Android instrumentation and the shell check, observing the current custom-scheme-only manifest/intake fail.
- [x] 3.4 GREEN: add the Android build-configured host placeholder, exact `autoVerify` intent filter, raw `dataString` intake, singleTop re-entry handling, and shared route configuration; rerun task 3.3 and the existing share/custom-link/notification intent suites.
- [x] 3.5 RED: add hosted XCTest cases in `IosLibraryRoutingTests.swift` named `testConfiguredUniversalLinksForwardRawAndResolveCanonicalDestinations`, `testForeignOrAmbiguousUniversalLinksAreRejected`, and `testBrowsingUserActivityUsesTheSharedRouteTable`, plus entitlement parity failure for a missing associated domain; run the hosted XCTest target and entitlement script through `build-gate` and observe the absent user-activity/entitlement wiring.
- [x] 3.6 GREEN: add the iOS `applinks:` build-setting entitlement, forward `NSUserActivityTypeBrowsingWeb` raw URLs into the shared configured parser, preserve `onOpenURL` custom-scheme behavior, and rerun task 3.5 plus unsigned simulator build and existing routing tests.

## 4. Notification availability, permission, and privacy

- [x] 4.1 RED: add compile-only notification policy ports and `shared/src/commonTest/kotlin/com/ratatoskr/mobile/notification/CompletionNotificationStoreTest.kt` cases `missing_public_subscribe_contract_is_integration_pending_and_calls_nothing`, `explicit_enable_requests_permission_once_when_contract_fixture_is_available`, `denied_permission_never_claims_delivery_or_reprompts`, `capability_loss_clears_stale_handle`, and `revocation_returns_empty_unpaired_state`; run the focused common test and observe policy/effect assertions fail against the unavailable placeholder.
- [x] 4.2 GREEN: implement separate contract-availability, preference, native-permission, and effective notification states with one-shot explicit effects and production `IntegrationPending` wiring that has no subscribe transport or token storage; rerun task 4.1 and identity/erasure tests to green.
- [x] 4.3 RED: extend `CaptureStatusNotificationTest` and add Android permission-host tests `explicit_available_enable_requests_post_notifications_once`, `denied_state_opens_settings_without_reprompt`, and `notification_route_and_payload_exclude_all_private_canaries`; run focused instrumentation and observe missing permission-state adapter and privacy assertions fail.
- [x] 4.4 GREEN: implement the Android native permission/settings adapter, connect it only to explicit available-state effects, retain generic immutable local operation notifications, and rerun task 4.3 plus notification/deep-link/clear-data suites.
- [x] 4.5 RED: add hosted `IosNotificationPermissionTests.swift` cases `testIntegrationPendingRequestsNothing`, `testExplicitAvailableEnableRequestsAuthorizationOnce`, `testDeniedStateDoesNotReprompt`, and `testRevocationCancelsPendingAndDeliveredNotifications`; run the hosted XCTest target and observe absent permission-state observations fail.
- [x] 4.6 GREEN: implement the iOS `UNUserNotificationCenter` permission/settings adapter and shared-state bridge without APNs registration or a guessed Platform call; rerun task 4.5 plus local-erasure and application smoke tests.

## 5. Accessibility, localization, and content-free diagnostics

- [x] 5.1 RED: add compile-only localized string/palette/diagnostic seams and common tests `MobileStringsTest.english_and_russian_cover_every_new_state_and_action`, `AccessiblePaletteTest.normal_large_and_control_colors_meet_required_ratios`, and `MobileDiagnosticsTest.private_canaries_cannot_enter_event_or_outcome_records`; run the focused common tests and observe missing-key, contrast, and placeholder-record assertions fail.
- [x] 5.2 GREEN: implement typed English/Russian catalogs with English fallback, tested accessible palette tokens, and a closed enum-only Kermit diagnostic boundary that accepts no free-form message, throwable, metadata, identifier, or content; rerun task 5.1 to green.
- [x] 5.3 RED: add Compose instrumentation cases covering search, library/read-state, operation partial/error, GitHub confirmation, notification settings, local-storage clear, and primary navigation; assert headings, roles, names, state descriptions/live regions, logical traversal, scalable unclipped text, and 48 dp targets, then run them and observe the current BasicText/Box semantics fail.
- [x] 5.4 GREEN: introduce shared accessible heading/status/input/action/result primitives, move every touched string through the locale catalog, and adopt them across the task 5.3 surfaces without changing domain behavior; rerun task 5.3 plus existing Compose UI suites and inspect EN/RU phone fixtures on the emulator/simulator.
- [x] 5.5 RED: add `tooling/tests/privacy_source_gate_test.sh` with mutation self-tests for Android `Log`, `println`, Swift `print`/`NSLog`, raw throwable/crash/breadcrumb metadata, and forbidden content fields, plus behavior canaries through search/link/notification/error flows; run it and confirm each injected production-source violation is rejected while the current approved tree is classified deterministically.
- [x] 5.6 GREEN: wire closed diagnostics at the affected boundaries, remove or isolate any direct production logging found by task 5.5, add the privacy source gate to repository checks, and rerun the source mutation tests and content-canary suites to green.

## 6. Shell smoke, documentation, and CI parity

- [x] 6.1 RED: extend the deterministic Android and iOS shell smoke suites to exercise paired search, canonical HTTPS analysis/collection/repository dispatch, notification integration-pending/permission states, Russian labels, accessibility semantics, and private-canary absence; run each focused suite through `build-gate` and observe missing end-to-end graph wiring rather than environment failure.
- [x] 6.2 GREEN: finish long-lived Android/iOS graph wiring for search, configured routes, notification policy, locale selection, and diagnostics; rerun task 6.1 plus shared framework linkage and both unsigned application builds.
- [x] 6.3 RED: extend `tooling/tests/gate_parity_test.sh`, shell/entitlement checks, and CI marker assertions to require the new shared search/routing/notification/locale/privacy suites, Android App Link/a11y smoke, and iOS Universal Link/permission/a11y smoke; run the parity test before editing `DEVELOPMENT.md` or CI and observe every missing command/marker diagnostic.
- [x] 6.4 GREEN: add the exact task 6.3 commands to `DEVELOPMENT.md` and `.github/workflows/ci.yml`, preserving the machine-wide `build-gate` rule locally and hosted Android/iOS test/build families; rerun parity, shellcheck, actionlint, Swift format lint, Kotlin lint, and strict OpenSpec validation.
- [x] 6.5 Update README, ARCHITECTURE, INTERFACES, THREAT_MODEL, TESTING, IMPLEMENTATION_PLAN item 9, and `docs/ACCESSIBILITY_PRIVACY_CHECKLIST.md` with observed automated/manual results, EN/RU state coverage, permissions/entitlements, no-content diagnostics, rollback, and exact fixture/emulator/simulator versus live-domain/push/physical-device/store boundaries; no meaningful failing behavior test applies because these are required evidence and architecture records, then verify links/commands and rendered Markdown.

## 7. Full gate, review, and OpenSpec completion

- [x] 7.1 Run every command in `DEVELOPMENT.md`, routing all Gradle and Xcode builds/tests through `build-gate`, including contract drift/mutation, Kotlin/Swift lint, shared Android/JVM and iOS Simulator tests, Android emulator instrumentation, hosted XCTest, both app builds, accessibility/privacy gates, strict OpenSpec validation, `git diff --check`, and workflow lint; record exact outcomes without relabeling fixture/simulator evidence as live Platform, deployed association, APNs/FCM, physical-device, signing, or store proof.
- [x] 7.2 Review the complete diff for invented endpoints/capabilities/domains, tenant selectors, stale-query races, mixed search authority, permissive URL normalization, link content leakage, unsolicited permission prompts, token/provider additions, PII in logs/crash metadata/notifications, inaccessible actions, untranslated touched strings, database migration/version changes, unrelated edits, and stale call sites; fix every finding and rerun its narrow test plus affected gate.
- [x] 7.3 Sync all four deltas into main specs, verify delta/main requirement equivalence, rerun `openspec validate --all --strict` and `openspec validate --archived`, confirm every task is checked, and archive the completed change; repository delivery follows the requested Git workflow after the OpenSpec lifecycle is complete.
