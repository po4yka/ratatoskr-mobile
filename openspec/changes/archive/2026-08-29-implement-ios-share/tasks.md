## 1. Testable extension and iOS target seams

- [x] 1.1 Add the Share Extension target, hosted extension test target, source groups, activation plist, scheme membership, and behavior-free parser/inbox public type seams so RED tests compile; configuration/scaffolding cannot begin with a behavior test, and `xcodebuild -list -project iosApp/Ratatoskr.xcodeproj` must enumerate the new targets without building feature behavior.
- [x] 1.2 Add `iosApp/RatatoskrShareTests/ShareExtensionParserTests.swift` tests `url_representation_preserves_original`, `plain_text_single_url_is_detected`, `plain_text_without_url_is_preview_only`, `equivalent_representations_are_deduplicated`, `multiple_distinct_urls_are_rejected`, `non_http_scheme_is_rejected`, `oversized_text_is_rejected`, `provider_failure_is_visible`, and `deadline_completes_once`; run the focused simulator XCTest and observe an assertion failure from the non-functional parser seam.
- [x] 1.3 Implement bounded async `NSItemProvider` URL/plain-text parsing, semantic deduplication, validation, cancellation/deadline handling, safe error presentation, and exactly-once extension-context completion; rerun `ShareExtensionParserTests` and verify the full matrix passes.

## 2. Atomic App Group handoff and import

- [x] 2.1 Add `iosApp/RatatoskrShareTests/AppGroupEnvelopeTests.swift` tests `published_envelope_round_trips_exactly`, `temporary_write_is_not_importable`, `oversized_or_malformed_envelope_is_refused`, `filename_and_identifier_must_match`, `envelope_contains_no_identity_or_credential_fields`, and `publish_failure_does_not_complete_successfully`; run them and observe assertion failures from the behavior-free inbox seam.
- [x] 2.2 Implement the bounded schema-1 envelope codec, protected same-volume temporary write, synchronization/atomic publish, allowlisted UUID filenames, exact field validation, and safe publish failure; rerun `AppGroupEnvelopeTests` and verify they pass.
- [x] 2.3 Add `iosApp/RatatoskrTests/AppGroupInboxImporterTests.swift` tests `published_item_is_claimed_once`, `temporary_and_unrelated_files_are_ignored`, `concurrent_import_claims_once`, `cancel_removes_only_claimed_item`, `queue_failure_keeps_claim_recoverable`, and `restart_recovers_processing_item`; run the hosted XCTest and observe assertion failures from the importer seam.
- [x] 2.4 Implement serialized published-to-processing rename claims, bounded rejection handling, cancel/commit cleanup callbacks, restart recovery, and app-owned-path enforcement; rerun `AppGroupInboxImporterTests` and verify all handoff integrity cases pass.

## 3. Stable shared staging and queue identity

- [x] 3.1 Extend only the compile seam of `ShareStagingStore` for caller-supplied source, capture time, idempotency key, and terminal callbacks, then add common tests in `shared/src/commonTest/kotlin/com/ratatoskr/mobile/share/ShareStagingViewModelTest.kt`: `ios_confirmation_uses_handoff_identity_and_source`, `ios_restart_converges_on_existing_queue_record`, `ios_id_reuse_with_changed_payload_fails_closed`, `cancel_invokes_cleanup_without_queueing`, and `queue_failure_retains_handoff`; run `:shared:testDebugUnitTest` and observe behavioral assertion failures while existing Android tests still compile.
- [x] 3.2 Implement source/time/key propagation and exactly-once commit/cancel/failure callbacks without changing Android defaults; rerun shared staging and queue suites on JVM and iOS Simulator and verify stable idempotency, provenance, cleanup, and existing Android behavior pass.

## 4. iOS application graph, submission, and status

- [x] 4.1 Add an `iosMain` application-facade compile seam plus `shared/src/iosTest/kotlin/com/ratatoskr/mobile/IosApplicationGraphTest.kt` tests `foreground_reconcile_drains_eligible_handoff`, `offline_result_preserves_next_queue_time`, `uncertain_restart_reuses_request_and_key`, `revocation_stops_authorization_retry`, and `background_expiration_leaves_claim_recoverable`; run `:shared:iosSimulatorArm64Test` and observe behavioral assertion failures.
- [x] 4.2 Implement the iOS application graph around the existing private Room queue, device session manager, authorized capture client, bounded submission coordinator, operation refresh, cancellation, and queue-derived next wake time; rerun `IosApplicationGraphTest`, shared submission, queue persistence, refresh-rotation, and revocation suites and verify they pass.
- [x] 4.3 Add `iosApp/RatatoskrTests/IosSubmissionStatusFlowTests.swift` tests `imported_url_requires_confirmation`, `confirmed_url_reports_queued_offline`, `fixture_acceptance_opens_operation_detail`, `running_partial_failed_cancelled_and_completed_fixtures_render`, `scene_inactive_stops_polling`, and `reauth_state_is_actionable`; run hosted XCTest and observe assertion failures because the Swift shell does not yet supply the application graph or lifecycle to shared Compose.
- [x] 4.4 Wire the Swift scene container and shared Compose root to imported handoffs, current pairing/capability state, queue scheduling, operation list/detail stores, monotonic fixture projection, and active/inactive polling visibility; rerun `IosSubmissionStatusFlowTests` and verify submission/status behavior mirrors Android at the shared UI seam.

## 5. Native background wake-up

- [x] 5.1 Add `iosApp/RatatoskrTests/IosSubmissionSchedulerTests.swift` tests `registration_uses_reviewed_identifier`, `schedule_uses_persisted_earliest_time`, `duplicate_requests_are_coalesced`, `early_wake_submits_nothing`, `expiration_cancels_drain`, `revocation_completes_without_reschedule_storm`, and `scene_activation_repairs_missing_request`; run hosted XCTest with a fake BackgroundTasks boundary and observe behavioral assertion failures.
- [x] 5.2 Implement native `BGAppRefreshTask` registration, queue-derived request scheduling/coalescing, bounded graph drain, expiration cancellation, completion mapping, and mandatory scene-activation reconciliation without capture content or credentials in task metadata; rerun `IosSubmissionSchedulerTests` and verify all wake-up cases pass.

## 6. App Group and Keychain entitlement boundary

- [x] 6.1 Add `tooling/tests/ios_share_entitlements_test.sh` assertions for exact app/extension/test App Group and Keychain group values, absence of wildcard/unrelated groups, extension embedding, and required background identifier; run it and observe the expected missing-entitlement/target assertion failure.
- [x] 6.2 Add checked-in entitlements and build settings for `group.com.ratatoskr.mobile`, `$(AppIdentifierPrefix)com.ratatoskr.mobile.shared`, extension embedding, test hosts, and the background task identifier; rerun `ios_share_entitlements_test.sh` and verify structural configuration passes.
- [x] 6.3 Add explicit access-group compile input to `IosKeychainCredentialStorage` plus hosted tests in `iosApp/RatatoskrTests/IosKeychainCredentialStorageTests.swift`: `configured_group_round_trip_replace_and_delete`, `device_only_non_synchronizing_policy_is_preserved`, and `wrong_group_cannot_read_item`; run focused XCTest and observe behavioral assertion failures while the query still ignores the group.
- [x] 6.4 Include the explicit access group in every Security query, supply it from the main iOS graph, keep the extension production flow free of Keychain calls, and rerun hosted Keychain plus effective-entitlement tests to verify isolation, replacement, device-only policy, and deletion.

## 7. Simulator smoke and full product gate

- [x] 7.1 Add `iosApp/RatatoskrTests/IosShareSmokeTests.swift` test `synthetic_share_survives_extension_graph_termination_and_queue_reopen_then_renders_terminal_status`; run it on a dedicated simulator and observe an assertion failure at the first missing end-to-end wiring boundary, not a compile or fixture error.
- [x] 7.2 Complete the actual extension-to-App-Group-to-main-app-to-Room-to-fixture-operation wiring and scheme embedding needed by the smoke; rerun it after explicitly terminating the extension-side graph, reopen the queue, inspect effective `.app`/`.appex` entitlements, and retain a content-free `.xcresult` showing the smoke passes.
- [x] 7.3 Add a failing `tooling/tests/gate_parity_test.sh` expectation that the documented and hosted gate runs parser, handoff, entitlement, Keychain, submission/status, embedded-extension build, and simulator smoke suites; run it and observe the missing-gate assertion before editing CI/docs.
- [x] 7.4 Update ADRs, `DEVELOPMENT.md`, `docs/TESTING.md`, `docs/IMPLEMENTATION_PLAN.md`, the Xcode scheme, and `.github/workflows/ci.yml` with exact iOS share commands/evidence boundaries, then rerun the parity test and Swift lint; documentation/configuration itself has no additional behavior test beyond the parity and build checks.
- [x] 7.5 Run the complete `DEVELOPMENT.md` product gate through `build-gate --` for every Gradle/Xcode/heavy test command, including shared Android/iOS suites, Android emulator suites, extension/application simulator XCTest and smoke, unsigned embedded-extension build, generated-contract drift/mutation, strict current and archived OpenSpec validation, shell tests, Swift lint, and `git diff --check`; record exact simulator/fixture evidence and any physical-device/live-Platform/signing gap.
- [x] 7.6 Review the final diff for unrelated edits, payload/credential leakage, symlink/path traversal, duplicate completion/submission, lifecycle leaks, accessibility regressions, stale generated files, and missing call sites; verify every task is checked only after its named RED and GREEN runs were observed.
