## 1. Persistence and public seams

- [x] 1.1 Add pinned Room compiler/runtime and bundled SQLite dependencies, apply KSP to the shared module, and add behavior-free `CaptureQueue`, `QueuePersistence`, clock/key/jitter, and native database-factory seams; verify `build-gate -- ./gradlew --no-daemon :shared:compileDebugKotlinAndroid :shared:compileKotlinIosSimulatorArm64` succeeds. This build/source-set wiring has no behavior to drive with a failing test and exists so subsequent tests fail at runtime rather than compilation.

## 2. Shared capture and operation behavior

- [x] 2.1 Add `shared/src/commonTest/kotlin/com/ratatoskr/mobile/capture/CaptureModelsTest.kt` tests `supported_capture_kinds_round_trip` and `invalid_capture_input_is_rejected_atomically`; run `build-gate -- ./gradlew --no-daemon :shared:testDebugUnitTest :shared:iosSimulatorArm64Test` and observe the round-trip/validation assertions fail at the behavior-free capture seam.
- [x] 2.2 Implement immutable owner/source/capture/payload types, canonical HTTPS owner origin, bounded URL/text/file validation, deterministic equality encoding, and safe serialization; rerun the focused shared tests and observe them pass.
- [x] 2.3 Add `shared/src/commonTest/kotlin/com/ratatoskr/mobile/queue/BackoffPolicyTest.kt` tests `retry_delay_progresses_with_equal_jitter_and_caps`, `later_server_hint_delays_retry_within_bound`, and `permanent_failure_has_no_retry_time`; run the focused shared tests and observe the expected 15-second, 30-second, 60-second, cap, hint, and no-retry assertions fail.
- [x] 2.4 Implement saturating equal-jitter exponential backoff, bounded server hints, retry exhaustion, and retry/permanent/auth failure classification; rerun the focused tests and observe them pass.
- [x] 2.5 Add `shared/src/commonTest/kotlin/com/ratatoskr/mobile/operation/OperationProjectionTest.kt` tests `newer_snapshots_advance_to_terminal`, `duplicate_and_older_snapshots_do_not_regress_terminal`, and `equal_time_conflict_is_refused`; run the focused shared tests and observe projection assertions fail at the explicit unimplemented projection seam.
- [x] 2.6 Implement the minimized generated-contract operation projection with operation-ID matching, timestamp ordering, duplicate/out-of-order suppression, equal-time conflict reporting, and terminal non-regression; rerun shared Android/JVM and iOS Simulator tests and observe them pass.

## 3. Durable capture queue

- [x] 3.1 Add `shared/src/androidInstrumentedTest/kotlin/com/ratatoskr/mobile/queue/AndroidCaptureQueuePersistenceTest.kt` tests `queue_survives_close_and_reopen` and `idempotency_key_stays_stable_across_reopen`, plus the matching tests in `shared/src/iosTest/kotlin/com/ratatoskr/mobile/queue/IosCaptureQueuePersistenceTest.kt`; run the Android instrumentation and iOS Simulator test commands and observe runtime persistence assertions fail at the behavior-free store seam rather than from compilation.
- [x] 3.2 Implement the single current Room schema, transactional DAO/store, generated database constructor, Android private backup-disabled factory, and iOS path/protection factory; rerun both native persistence suites and observe the original payload, source sequence, local ID, and idempotency key survive close/reopen.
- [x] 3.3 Add `shared/src/commonTest/kotlin/com/ratatoskr/mobile/queue/CaptureQueueIdempotencyTest.kt` tests `matching_idempotency_key_returns_existing_record`, `different_payload_for_existing_key_is_refused`, `full_queue_keeps_existing_records`, and `ready_work_is_owner_scoped`; run the focused shared tests and observe count, conflict, preservation, and isolation assertions fail.
- [x] 3.4 Implement atomic enqueue, database-enforced key/sequence uniqueness, matching-request convergence, key-conflict refusal, configured record/payload bounds, and explicit owner-scope queries; rerun the focused shared and native store tests and observe them pass.
- [x] 3.5 Add `shared/src/commonTest/kotlin/com/ratatoskr/mobile/queue/CaptureQueueOrderingTest.kt` tests `dequeue_is_fifo_within_source`, `waiting_source_does_not_block_other_source`, `expired_claim_reopens_after_restart`, and `stale_claim_token_cannot_complete_new_attempt`; run the focused shared tests and observe ordering/lease assertions fail.
- [x] 3.6 Implement transactional per-source sequences, lane-head selection, deterministic cross-lane ordering, finite claim leases, expired-claim recovery, and stale-token rejection; rerun shared tests and the native close/reopen contract and observe them pass.
- [x] 3.7 Add `shared/src/commonTest/kotlin/com/ratatoskr/mobile/queue/CaptureQueueRetryTest.kt` tests `retry_outcome_persists_attempt_and_next_eligible_time`, `server_hint_controls_dequeue_eligibility`, `retry_exhaustion_stops_automatic_work`, and `permanent_failure_is_never_dequeued`; run the focused shared tests and observe them pass. The retry outcome seam was introduced test-first by 3.5/3.6 so FIFO tests could complete claims, while the backoff algorithm had its own red tests in 2.3.
- [x] 3.8 Integrate retry policy with claim outcomes and transactional queue state, preserving per-source order and excluding permanent/auth/exhausted work from automatic dequeue; rerun focused, shared-target, and native persistence tests and observe them pass.
- [x] 3.9 Add `shared/src/commonTest/kotlin/com/ratatoskr/mobile/queue/CaptureQueueResolutionTest.kt` tests `existing_server_operation_converges_after_uncertain_response`, `repeated_identical_acceptance_is_idempotent`, `different_operation_for_same_key_fails_closed`, and `bound_operation_snapshot_updates_persisted_projection`; run the focused shared tests and observe binding/count/conflict/projection assertions fail.
- [x] 3.10 Implement authoritative operation binding, repeated-acceptance convergence, conflicting-operation preservation, automatic-work exclusion, and persisted projection application; rerun all shared queue tests on Android/JVM and iOS Simulator plus native close/reopen tests and observe them pass.

## 4. Architecture decision and product gate

- [x] 4.1 Extend `tooling/tests/architecture_boundary_test.sh` with `adr_0004_records_current_schema_and_queue_semantics` and `tooling/tests/gate_parity_test.sh` with assertions for capture-queue/current-schema tests on Android and iOS; run both scripts and observe the purpose-specific ADR and CI/documented-gate assertions fail against the pre-change tree.
- [x] 4.2 Add accepted `docs/adr/0004-durable-offline-capture-queue.md`, update the ADR index, architecture/data-model/testing/threat-model/development/README documentation, CI commands, and implementation-plan status, then rerun the repository scripts until they pass. Documentation and CI configuration do not begin with unit tests; the executable boundary/parity scripts provide their red-green evidence.

## 5. Full validation and lifecycle readiness

- [x] 5.1 Run every command in `DEVELOPMENT.md`, routing all Gradle and Xcode builds/tests through `build-gate`, plus contract drift/mutation checks, Swift format lint, actionlint, `git diff --check`, `openspec validate --all --strict`, and `openspec validate --archived`; inspect the complete diff for user-content leakage, schema migration artifacts, generated-contract drift, scope creep, and missing Android/iOS call sites.
- [x] 5.2 Sync both delta specs into the main specs, verify delta/main equivalence, rerun strict OpenSpec validation, and confirm every task is checked and the change is ready for archival; archive, commit, main integration, push, exact hosted-SHA verification, worktree removal, and task-branch deletion follow the lifecycle after implementation.
