## 1. Build wiring and public seams

- [x] 1.1 Pin AndroidX WorkManager runtime/testing and Compose Android UI-test dependencies in the version catalog, add `androidApp` unit/instrumentation source sets plus test runner configuration, and add behavior-free public intake, scheduler, notification, authorized-request, capture-submission, and operation-repository seams; verify `build-gate -- ./gradlew --no-daemon :shared:compileDebugKotlinAndroid :shared:compileKotlinIosSimulatorArm64 :androidApp:assembleDebug :androidApp:assembleDebugAndroidTest` succeeds. This configuration/seam task has no behavior to drive with a failing test and exists so each following red assertion compiles.

## 2. Native Android share intake

- [x] 2.1 Add `androidApp/src/androidTest/kotlin/com/ratatoskr/mobile/share/AndroidShareIntentParserTest.kt` tests `single_url_is_staged_without_enqueue`, `title_and_one_url_preserve_original_text`, `plain_text_is_previewed_as_contract_unavailable`, and `hostile_or_ambiguous_intents_are_rejected`; run `build-gate -- ./gradlew --no-daemon :androidApp:connectedDebugAndroidTest` and observe URL detection, original-text preservation, unsupported-text, and rejection assertions fail at the behavior-free parser seam.
- [x] 2.2 Implement bounded native `ACTION_SEND`/`text/plain` parsing with exact single-HTTP(S)-URL extraction, original text preservation, unsupported plain-text preview, and safe rejection without reading streams/clipboard; rerun the parser instrumentation class and observe the full matrix pass.
- [x] 2.3 Add `tooling/tests/android_share_manifest_test.sh` test `manifest_declares_only_explicit_text_share_target` and run it to observe assertions fail because the activity has no `ACTION_SEND`/`text/plain` filter or internal status-detail action contract.
- [x] 2.4 Update the Android manifest/activity launch handling for explicit `ACTION_SEND` and validated internal status-detail intents, retaining backup-disabled/private behavior and excluding file MIME filters; rerun the manifest test and `build-gate -- ./gradlew --no-daemon :androidApp:assembleDebug` until both pass. Manifest configuration cannot itself begin with a Kotlin unit test; task 2.3 is its executable red check.

## 3. Shared staging and durable confirmation

- [x] 3.1 Add `shared/src/commonTest/kotlin/com/ratatoskr/mobile/share/ShareStagingViewModelTest.kt` tests `confirm_commits_before_scheduler`, `cancel_persists_nothing`, `offline_confirmation_reports_durable_queue_state`, `unsupported_plain_text_cannot_submit`, and `repeat_confirm_does_not_duplicate`; run `build-gate -- ./gradlew --no-daemon :shared:testDebugUnitTest :shared:iosSimulatorArm64Test` and observe state/order/idempotency assertions fail at the behavior-free staging seam.
- [x] 3.2 Implement shared UDF staging state/actions, current-owner URL request creation, serialized queue confirmation, cancel/unsupported behavior, and the content-free scheduler call after durable success; rerun the focused shared staging tests on Android/JVM and iOS Simulator and observe them pass.
- [x] 3.3 Add `androidApp/src/androidTest/kotlin/com/ratatoskr/mobile/share/ShareStagingUiTest.kt` tests `url_preview_requires_confirm`, `plain_text_explains_unavailable_contract`, and `queued_state_is_visible_offline`; run the Android instrumentation suite and observe the required Compose semantics/text/actions fail before staging UI exists.
- [x] 3.4 Implement accessible shared Compose staging presentation and Navigation 3 staging route without note/tag/collection fields, wire it from the native intake result, and rerun the staging UI and shared state suites until they pass.

## 4. Authenticated offline submission

- [x] 4.1 Add `shared/src/commonTest/kotlin/com/ratatoskr/mobile/submission/PlatformCaptureApiTest.kt` tests `submit_sends_generated_body_and_persisted_key`, `accepted_fixture_decodes`, `retryable_statuses_and_retry_hint_are_classified`, `permanent_validation_is_classified`, and `invalid_success_fails_closed`; run focused shared tests and observe request/response/classification assertions fail at the behavior-free typed API seam.
- [x] 4.2 Implement the dedicated Ktor capture adapter using generated `SubmitCapture`/`CaptureAccepted`, bearer/idempotency headers, bounded retry hints, safe response validation, and distinct transport/domain outcomes; rerun the capture API fixtures on Android/JVM and iOS Simulator and observe them pass.
- [x] 4.3 Add `shared/src/commonTest/kotlin/com/ratatoskr/mobile/submission/CaptureSubmissionCoordinatorTest.kt` tests `accepted_claim_binds_original_operation`, `uncertain_restart_reuses_body_and_key`, `connectivity_persists_retry_time`, `permanent_failure_stops`, and `revocation_preserves_auth_required_capture`; run focused shared tests and observe queue transition, authorization recovery, and no-replacement assertions fail.
- [x] 4.4 Implement authorized request execution plus bounded capture submission orchestration over `DeviceSessionManager` and `CaptureQueue`, including one serialized refresh/recovery after `401`, exact queue-failure mapping, acceptance binding, and revocation handling; rerun the focused coordinator and existing identity/queue suites until they pass.
- [x] 4.5 Add `shared/src/commonTest/kotlin/com/ratatoskr/mobile/queue/CaptureQueueWorkQueryTest.kt` tests `next_wake_time_is_owner_scoped_and_durable`, `accepted_operations_are_refreshable_not_submittable`, and `terminal_refresh_unblocks_source_lane`; run focused shared tests and observe work-query/ordering assertions fail against the current queue API.
- [x] 4.6 Implement owner-scoped pending-submission/operation-refresh queries and deterministic next-wake calculation in the current Room-backed queue without a schema migration; rerun work-query, ordering, resolution, and native close/reopen suites until they pass.
- [x] 4.7 Add `androidApp/src/androidTest/kotlin/com/ratatoskr/mobile/submission/CaptureSubmissionWorkerTest.kt` tests `worker_uses_connected_unique_work_without_content_data`, `worker_honors_persisted_delay`, `process_recreation_resumes_queue`, and `revocation_finishes_without_retry_storm`; run Android instrumentation and observe WorkManager constraint/delay/recovery/outcome assertions fail.
- [x] 4.8 Implement the manual application container, unique connected WorkManager scheduler/worker, bounded submission and accepted-operation refresh drain, startup/re-pair reconciliation, and content-free work metadata; rerun the WorkManager instrumentation plus shared submission/queue tests until they pass.

## 5. Operation list, detail, and bounded polling

- [x] 5.1 Add `shared/src/commonTest/kotlin/com/ratatoskr/mobile/operation/PlatformOperationsApiTest.kt` tests `operation_list_fixture_preserves_platform_order`, `detail_fixtures_decode_all_terminal_outcomes`, `not_found_is_non_enumerating`, and `invalid_snapshot_fails_closed`; run focused shared tests and observe fixture decoding/error-boundary assertions fail at the behavior-free operations API seam.
- [x] 5.2 Implement dedicated Ktor operation list/detail adapters with generated `OperationList`, `OperationSummary`, and `OperationSnapshot`, opaque cursor handling, validated UUID requests, authorization recovery, and safe error mapping; rerun the operations API fixtures on Android/JVM and iOS Simulator and observe them pass.
- [x] 5.3 Add `shared/src/commonTest/kotlin/com/ratatoskr/mobile/operation/OperationStatusViewModelTest.kt` tests `list_exposes_loading_empty_fixture_and_offline_states`, `running_detail_advances_monotonically_to_terminal`, `older_snapshot_does_not_regress`, `polling_stops_when_hidden_or_terminal`, and `failure_cap_requires_manual_retry`; run focused shared tests and observe Turbine state/timing/cancellation assertions fail.
- [x] 5.4 Implement shared UDF operation list/detail state, safe presentation mapping, injected polling clock/delays, lifecycle visibility input, monotonic projection application, and bounded retry/manual refresh; rerun status-state and existing operation-projection suites until they pass.
- [x] 5.5 Add `androidApp/src/androidTest/kotlin/com/ratatoskr/mobile/operation/OperationStatusUiTest.kt` tests `fixture_list_renders_running_partial_failed_and_completed`, `detail_renders_progress_counts_and_safe_errors`, and `offline_and_reauth_states_are_actionable`; run instrumentation and observe required Compose semantics and outcome labels fail.
- [x] 5.6 Implement shared Compose Navigation 3 status list/detail surfaces with accessible loading/empty/offline/partial/error states, bounded progress presentation, and pairing/staging/status navigation; rerun status UI, staging UI, and shared state suites until they pass.

## 6. Privacy-safe notifications and deep-link routing

- [x] 6.1 Add `androidApp/src/androidTest/kotlin/com/ratatoskr/mobile/notification/CaptureStatusNotificationTest.kt` tests `notification_contains_no_capture_content`, `immutable_explicit_intent_opens_authorized_detail`, `permission_denial_does_not_change_queue`, and `invalid_identifier_issues_no_fetch`; run instrumentation and observe notification content/flags/route validation assertions fail at the behavior-free adapter seam.
- [x] 6.2 Implement the generic Android status notification channel/adapter, permission-safe posting, explicit immutable pending intents carrying only an operation UUID, activity route validation, and shared detail handoff; rerun notification, deep-link, and status-detail suites until they pass.

## 7. Emulator smoke, ADR, and product gate

- [x] 7.1 Add `androidApp/src/androidTest/kotlin/com/ratatoskr/mobile/smoke/AndroidShareSmokeTest.kt` test `share_url_confirms_queues_accepts_and_opens_terminal_status`, using only synthetic `ACTION_SEND` data and deterministic Platform fixtures; run `build-gate -- ./gradlew --no-daemon :androidApp:connectedDebugAndroidTest` and observe the end-to-end activity/queue/worker/status/notification assertion fail before final application wiring and fixture injection are complete.
- [x] 7.2 Complete application-container/activity/shared-root wiring and the deterministic emulator fixture profile, record the screenshot/test-report artifact without content-bearing logs, then rerun the smoke and all Android instrumentation until the share-to-queued/accepted/terminal-detail flow passes across activity/database reopen.
- [x] 7.3 Extend `tooling/tests/architecture_boundary_test.sh` with `adr_0002_records_android_share_lifecycle_boundary` and `tooling/tests/gate_parity_test.sh` with Android share/staging/submission/status/deep-link/smoke command assertions; run both scripts and observe the purpose-specific ADR and CI/documented-gate assertions fail against the pre-documentation tree.
- [x] 7.4 Add accepted ADR-0002, update the ADR index, architecture/interfaces/testing/threat-model/development/README documentation and item-4 status, add Android instrumentation/smoke artifact execution to CI, and rerun the boundary/parity scripts until they pass. Documentation and CI configuration do not begin with unit tests; task 7.3 is their executable red-green evidence.
- [x] 7.5 Add Android smoke assertion `hostile_share_displays_safe_actionable_error`; run it and observe rejected intake fall through to the identity surface instead of the required error.
- [x] 7.6 Render a shared safe rejection surface for native parser failures and rerun the focused and full Android instrumentation suites until the error is visible without queue work.
- [x] 7.7 Add Android lifecycle assertion `operation_polling_stops_when_activity_is_not_resumed`; run it and observe detail polling continue after the Activity leaves `RESUMED`.
- [x] 7.8 Bind shared detail visibility to the Android lifecycle as well as route composition and rerun the focused and full Android instrumentation suites until polling cancels off-resume and restarts on resume.
- [x] 7.9 Extend status UI fixtures with `offline_list_retains_stale_context` plus list stage/progress/change-time assertions; run them and observe the offline refresh discard the last presentation and the list omit contract fields.
- [x] 7.10 Retain clearly marked in-memory list context through an offline refresh, render stage/progress/change time, and rerun focused/full status suites until the generated fixture remains truthful and visible.
- [x] 7.11 Add the behavior-free shared submission-access state seam, then add `submit_capability_controls_confirmation_and_recovers`; run it and observe staging remain enabled while `content.submit` is unavailable.
- [x] 7.12 Drive staging availability from the current paired session and fresh `content.submit` capability, fail closed while unavailable, recover reactively, and rerun shared plus Android staging/smoke suites.

## 8. Full validation and lifecycle completion

- [x] 8.1 Run every command in `DEVELOPMENT.md`, routing all Gradle and Xcode builds/tests through `build-gate`, plus contract drift/mutation, Swift format lint, actionlint, `git diff --check`, `openspec validate --all --strict`, and `openspec validate --archived`; inspect the complete diff for payload/token leakage, exported-component mistakes, unbounded polling/work, migration artifacts, generated-contract drift, iOS regressions, and scope creep.
- [x] 8.2 Sync all four delta specs into the main specs, verify delta/main equivalence, rerun strict OpenSpec validation, confirm every task is checked, and archive the change; then commit on the task branch, fast-forward or otherwise safely integrate it into `main`, push `main`, verify required hosted CI for the exact pushed SHA, remove the dedicated worktree, and delete the merged task branch.
