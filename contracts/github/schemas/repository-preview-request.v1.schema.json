{
  "$id": "urn:ratatoskr:contracts:github:v1:RepositoryPreviewRequest",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "RepositoryPreviewRequest",
  "description": "A read-only repository preview request.",
  "type": "object",
  "properties": {
    "repository_url": {
      "description": "Canonical GitHub repository URL supplied by the caller.",
      "$ref": "#/$defs/GitHubRepositoryUrl"
    }
  },
  "additionalProperties": false,
  "required": [
    "repository_url"
  ],
  "x-ratatoskr-provenance": {
    "canonical_source": "ratatoskr_github_contracts::RepositoryPreviewRequest",
    "contract_id": "github.repository_preview_request",
    "contract_major_version": 1,
    "generated": "DO NOT EDIT — generated by contractsc from the canonical Rust source. Change the Rust type and run `cargo contracts generate`.",
    "generator": "contractsc",
    "generator_version": "0.1.0",
    "schemars_version": "1.2.2",
    "source_digest": "sha256:eb5439ffe631bc3cb0adccf48e5c6ed97f4364260525a3f64a1b549ea23ee2f1",
    "validation_note": "This schema is a LOWER BOUND on validity. Cross-field invariants and canonical-form rules are enforced by the canonical Rust type; see fixtures/invalid-expectations.toml for which layer rejects what."
  },
  "$defs": {
    "GitHubRepositoryUrl": {
      "title": "GitHubRepositoryUrl",
      "description": "Canonical HTTPS GitHub repository URL with exactly an owner and repository path.",
      "type": "string",
      "examples": [
        "https://github.com/owner/repository"
      ],
      "maxLength": 220,
      "pattern": "^https://github\\.com/[A-Za-z0-9][A-Za-z0-9_.-]{0,99}/[A-Za-z0-9][A-Za-z0-9_.-]{0,99}$"
    }
  }
}
