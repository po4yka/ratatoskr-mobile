{
  "$id": "urn:ratatoskr:contracts:transfer:v1:UploadFinalizeRequest",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "UploadFinalizeRequest",
  "description": "Asks the receiver to verify and seal a completed session. Required after every derived chunk\nindex is recorded; premature calls are refused with\n`blob_transfer.finalize_incomplete` while the session stays open.",
  "type": "object",
  "properties": {
    "resumption_token": {
      "description": "The session to seal. Required.",
      "$ref": "#/$defs/UploadResumptionToken"
    }
  },
  "additionalProperties": true,
  "required": [
    "resumption_token"
  ],
  "x-ratatoskr-provenance": {
    "canonical_source": "ratatoskr_blob_transfer_contracts::UploadFinalizeRequest",
    "contract_id": "transfer.upload_finalize_request",
    "contract_major_version": 1,
    "generated": "DO NOT EDIT — generated by contractsc from the canonical Rust source. Change the Rust type and run `cargo contracts generate`.",
    "generator": "contractsc",
    "generator_version": "0.1.0",
    "schemars_version": "1.2.2",
    "source_digest": "sha256:26c60cf0bee396e9a91e3f602d9952356abbec5ed11f26152d0a6a4eb31e4467",
    "validation_note": "This schema is a LOWER BOUND on validity. Cross-field invariants and canonical-form rules are enforced by the canonical Rust type; see fixtures/invalid-expectations.toml for which layer rejects what."
  },
  "x-ratatoskr-unknown-policy": "preserve",
  "$defs": {
    "UploadResumptionToken": {
      "title": "UploadResumptionToken",
      "description": "Server-issued handle naming one upload session. Required on every chunk receipt, status\nand finalize call after opening.\n\nOpaque by contract: a client compares tokens, stores them and presents them back, and\nnever parses one. The issuing service mints it unguessable — at least 128 bits of entropy\n— because whoever holds the token drives the session; expiry is the server's decision,\ncarried beside the token as `expires_at`, and an expired token is refused with\n`blob_transfer.session_expired`, distinct from `blob_transfer.session_unknown`.",
      "type": "string",
      "examples": [
        "rst_0v8k4a2j9pm1d7n5tp3es6uabfhij1cm4nop5qrs"
      ],
      "maxLength": 68,
      "pattern": "^rst_[0-9a-v]{24,64}$"
    }
  }
}
